name: '💥 回归问题'
description: '之前能用的功能现在不能用了？请告诉我们！'
labels: ['needs triage', 'regression']
body:
  - type: checkboxes
    attributes:
      label: '是否已搜索过相关问题？'
      description: '请先搜索 [已有问题](./?q=is%3Aissue) 确认是否已有人报告'
      options:
        - label: '我已搜索过相关问题'
          required: true

  - type: input
    attributes:
      label: '可能引入问题的 PR/Commit'
      description: '如果你知道是哪个 PR 或 commit 引入的（可选）'
      placeholder: 'PR #123 或 commit hash'

  - type: textarea
    validations:
      required: true
    attributes:
      label: '问题描述'
      description: '请描述回归问题、重现步骤、之前的行为等（可直接拖拽文件附加截图或日志）'
      placeholder: |
        问题：之前...能正常工作，现在...不能用了
        重现步骤：...
        之前的行为：...
        现在的行为：...
        截图/日志：...
